flask
flask-cors
requests
cryptography
google-auth
openpyxl
python-docx
xlwt
# 💡 openpyxl/python-docx는 "승인원 표지 생성"(엑셀/워드 양식 채우기, 2026-09-15 신규)
# 전용 — 이 둘은 순수 Python 라이브러리라 SAP COM처럼 32/64비트를 가리지 않고 메인
# 64비트 환경(이 파일)에 바로 설치하면 된다.
# 💡 xlwt는 "구매오더 요청" 엑셀(레거시 BIFF .xls 포맷) 생성 전용(2026-09-15 신규) — openpyxl은
# 이 구형 포맷을 못 써서 별도로 필요.
# 💡 pywin32는 여기 없음 — SAP 조회(sap_bridge_32.py)는 SAP GUI Scripting의 COM이 32비트로만
# 등록돼 있어 별도 32비트 Python 환경에 따로 설치해야 한다(이 파일은 메인 64비트 환경용).
# kortek_backend.bat이 최초 실행 시 자동으로 처리한다: py install 3-32 && py -3-32 -m pip install pywin32
